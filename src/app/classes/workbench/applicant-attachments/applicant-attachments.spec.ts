import { ApplicantAttachments } from './applicant-attachments';

describe('ApplicantAttachments', () => {
  it('should create an instance', () => {
    expect(new ApplicantAttachments()).toBeTruthy();
  });
});
